public class AccessExample {
    // Non-static variable
    int number = 100;

    // Static method
    public static void printNumber() {
        // Attempt to access non-static variable directly (will cause an error)
        // System.out.println(number); // Error: Cannot make a static
reference to the non-static field number

        // Correct way: Access via an instance
        AccessExample obj = new AccessExample();
        System.out.println(obj.number); // Output: 100
    }

    public static void main(String[] args) {
        // Call static method
        printNumber();
    }
}
